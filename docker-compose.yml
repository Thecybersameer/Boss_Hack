version: '3.8'

services:
  app:
    image: nginx:stable-alpine
    container_name: boss_hack_app
    ports:
      - '8080:80'
    volumes:
      - ./html:/usr/share/nginx/html:ro
    restart: unless-stopped
